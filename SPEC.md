# 체크앤고 (Check & Go) — SPEC

Claude Code가 세션 내내 참고하는 마스터 스펙. CLAUDE.md보다 상위 개념 — "무엇을 왜 만들고, 뭐가 되면 완료인지"를 담음. 프로젝트 진행하며 계속 업데이트.

---

## 1. 개요

청년 정책 매칭 서비스. 자격 확인부터 서류 준비, 마감 관리까지 흩어져 있던 과정을 프로필 하나로 이어주는 서비스. MVP 타겟은 1인가구 미혼 청년. (자세한 배경: `docs/프로젝트_로드맵.md` PRD의 "배경/목적", "타겟·문제 정의")

---

## 2. 기능 요구사항 (완료 조건 포함)

> **진행 요약 (2026-07-16 기준)**: 기능(2-0~2-8) 구현·검증 완료 — 이메일 발송(2-9)만 포폴 범위 제외(설정 UI만). 브라우저 E2E 확인 완료. **🚀 배포 완료 — 라이브: https://checkngo.vercel.app** (Vercel 자동배포, Confirm email OFF). **배포 후 마감 작업**(§7): 데모 계정("데모로 둘러보기")·파비콘·링크 미리보기 카드·커스텀 404·UX 폴리싱·케이스 스터디(`docs/케이스스터디.md`) 완료. 남은 것(선택): docs IA 정리·커스텀 도메인.

### 2-0. 공통 헤더/네비게이션 (P0) — ✅ 구현 완료

- 모든 화면 상단에 공통 헤더 표시 (로그인 화면 제외)
- **비로그인 상태**: 로고("체크앤고")만 표시, 클릭 시 홈으로 이동
- **로그인 상태**: 로고 + 로그인한 이메일 + "프로필"·"마이페이지"·"캘린더" 링크 + "로그아웃" 버튼 표시
  - "프로필" 클릭 → 프로필 수정 화면으로 이동
  - "로그아웃" 클릭 → Supabase 세션 종료 후 로그인 화면(`/login`)으로 이동
- 헤더에 "캘린더"·"마이페이지" 진입점을 두어 어디서든 이동 가능하게 함 (화면플로우의 "언제든 이동 가능" 요구사항) — ✅ 구현됨

**완료 조건**: 로그인 여부에 따라 헤더 내용이 정확히 달라짐. 로그아웃 클릭 시 세션이 종료되고 로그인 화면으로 이동함. 헤더에서 캘린더·마이페이지·홈으로 각각 이동 가능함.

### 2-1. 로그인/회원가입 (P0) — ✅ 구현 완료

- 이메일/비밀번호 방식만 지원 (소셜로그인 없음)
- 로그인 / 회원가입 탭 전환 가능
- 이메일 형식이 아니면 에러 메시지 표시
- 비밀번호 길이가 Supabase 최소 기준 미달이면 에러 메시지 표시
- 로그인 실패(이메일/비밀번호 불일치) 시 "이메일 또는 비밀번호를 다시 확인해 주세요" 표시
- 회원가입 성공 시 자동 로그인 (Confirm email 비활성화 상태)
- 로그인 성공 시 `/home`으로 리다이렉트
- **로그아웃**: 공통 헤더의 "로그아웃" 버튼 클릭 시 Supabase 세션 종료, `/login`으로 이동 (2-0과 중복 정의 — 헤더 스펙이 기준)
- **데모 계정("데모로 둘러보기")**: 로그인 화면에 회원가입 없이 체험하는 버튼. 공개 데모 계정으로 자동 로그인(프로필·스크랩 미리 시드) → 즉시 맞춤 매칭 체험. 데모 로그인 상태엔 헤더에 "데모 체험 중" 배너 노출. (상세는 §7)

**완료 조건**: 회원가입 → 자동 로그인 → 로그아웃 → 재로그인이 에러 없이 동작. 이메일 형식 오류·비밀번호 오류·로그인 실패 3개 에러 케이스가 화면에 노출됨. 로그아웃 후 보호된 화면(프로필 등) 접근 시 로그인 화면으로 리다이렉트됨.

### 2-2. 프로필 입력 (P0) — ✅ 구현 완료

- 생년월일(date picker), 취업상태(단일선택), 소득수준(단일선택, "모름" 포함), 주거형태(단일선택), 거주지역(시/도 선택) 입력
- 5개 필드 모두 필수 입력 (미입력 시 저장 불가, 어떤 필드가 비었는지 표시)
- 저장 시 `profiles` 테이블에 upsert (이미 프로필 있으면 수정, 없으면 신규 생성)
- 생년월일로 나이를 직접 계산해서 저장하지 않음 — `birth_date`만 저장, 나이 구간은 매칭 시점에 계산 (정책 정의서 규칙)
- **거주지역은 전체 시/도를 다 선택 가능** — 서울 외 지역을 선택해도 온보딩이 막히지 않음. 국가 단위 정책은 지역 무관 정상 매칭되고, 지역 단위(서울) 정책만 결과 화면에서 "준비중"으로 표시됨 (정책 정의서 예외 처리 규칙). ⚠️ IA 문서의 "거주지역 확인 → 서울 외 지역 준비중 안내"는 이 로직이 확정되기 전 옛 버전이 그대로 남은 것 — IA 업데이트 필요, 구현은 정책 정의서 기준을 따를 것
- **재방문 시(이미 프로필 있음)**: 화면 진입 시 기존 저장값으로 폼이 미리 채워진 상태로 열림 (빈 폼 아님)

**완료 조건**: 5개 필드 다 입력하고 저장하면 Supabase `profiles` 테이블에 실제 행이 생성/갱신됨. 필수값 누락 시 저장 버튼이 막히거나 에러 표시. 기존 프로필이 있는 사용자가 재진입하면 저장된 값이 폼에 채워져 있음.

### 2-3. 홈 - 조건 매칭 필터링 (P0) — ✅ 구현 완료

- 로그인 + 프로필 있는 사용자: 매칭 로직(`matching.ts`) 실행 결과를 카드 리스트로 표시
- 비로그인 또는 프로필 없는 사용자: 전체 정책 카드 표시 (매칭 없이)
- **적용된 필터 조건을 칩(chip) 형태로 시각적으로 표시** — "내 맞춤만/전체보기" 토글만으로는 어떤 조건으로 걸러졌는지 안 보임. 정책 리스트 상단에 현재 프로필 기준 조건(나이 구간·취업상태·소득수준·주거형태·거주지역)을 칩으로 나열
  - MVP 범위: 칩은 현재 적용된 조건을 보여주는 용도(읽기 전용). 칩 또는 옆의 "필터 수정" 링크 클릭 시 프로필 수정 화면으로 이동해서 값 변경 (칩 자체를 눌러 그 자리에서 드롭다운으로 바로 수정하는 인라인 편집은 범위 밖 — 프로필 수정 화면과 로직이 중복되는 걸 피하기 위함, 추후 필요시 확장)
  - "전체보기(초기화)" 상태일 때는 칩 영역이 숨겨지거나 "필터 없음" 표시
- 정렬 기본값: 기본순. **마감 여부가 1차 기준** — 마감된 정책은 배지 상태와 무관하게 항상 최하단. 마감 안 된 것들끼리는 무배지 → 배지 → 소득초과 강한경고 → 준비중 순
- 정렬 옵션: 마감임박순 토글 가능 (진행중 회차마감 deadline 오름차순 → 상시 접수 → 마감됨 최하단)
- 마감된 정책: 카드 반투명 처리, **기본순·마감임박순 둘 다 하단 고정** (정렬 기준과 무관하게 항상 마지막)
- 매칭 결과 0건: 빈 상태 화면("조건 넓히기" 안내)
- 초기화 버튼: 클릭 시 프로필 필터링 무시하고 전체 정책 노출

**완료 조건**: 정책 정의서의 "예시 시뮬레이션" 표와 동일한 입력값을 넣었을 때 동일한 결과(매칭/준비중/미노출)가 화면에 나타남. (matching.ts 유닛 검증은 이미 완료 — 화면에서도 같은 결과인지 재확인) "내 맞춤만" 상태에서 현재 적용된 5개 조건이 칩으로 화면에 보이고, 클릭 시 프로필 수정 화면으로 이동함.

### 2-4. 정책 상세 (P0) — ✅ 구현 완료

- 정책명, 자격조건(나이/취업상태/소득/주거형태), 신청기간·마감일, 정책 설명(description) 표시
- 서류 체크리스트: 로그인 상태면 항목별 체크 가능(→ `scraps.checklist_status`에 저장), 비로그인이면 목록만 보임(체크 불가)
- 스크랩 버튼: 비로그인 시 클릭하면 로그인 유도, 로그인 상태면 스크랩 on/off 토글 (`scraps` 테이블에 저장/삭제)
- 신청 사이트 바로가기: 새 탭으로 `apply_url` 열기

**완료 조건**: 정책 상세 진입 시 해당 정책의 모든 필드가 정확히 표시됨. 로그인 상태에서 스크랩 토글이 DB에 반영됨(새로고침해도 유지). 신청 사이트 버튼이 실제 URL로 정상 연결됨.

### 2-5. 공통 UI 상태 규칙 (모든 화면 공통) — ✅ 구현·검증 완료

- **로딩 중**: 데이터 요청 중엔 스피너 또는 스켈레톤 UI 표시 (빈 화면 방치 금지)
- **에러 발생**: 사용자에게 이해 가능한 문구로 표시 ("문제가 발생했어요, 다시 시도해주세요" 등). 콘솔 에러 메시지를 그대로 화면에 노출하지 않음
- **반응형**: 모바일 화면(360px 이상)에서도 레이아웃이 깨지지 않아야 함 (MVP 스코프 "플랫폼: 웹(반응형)" 기준). 데스크탑 전용으로 만들지 않음

**완료 조건**: 네트워크를 느리게 시뮬레이션했을 때 로딩 표시가 보임. 의도적으로 에러를 발생시켰을 때 사용자 친화적 메시지가 뜸. 모바일 너비에서 주요 화면(로그인, 홈, 정책상세)이 깨지지 않고 보임.

### 2-6. 캘린더 (P1) — ✅ 구현 완료

- 월 단위 달력 그리드(일~토 7열). 상단에 "YYYY.MM" 표시 + 이전/다음 달 이동 화살표
- 오늘 날짜는 색상으로 강조 표시
- **각 날짜 칸 안에 그날이 마감일인 정책들을 리스트로 직접 표시** (날짜 클릭 후 별도 패널이 아니라, 칸 안에 바로 나열)
  - 정책마다: 구분 배지 아이콘(국가/서울) + 정책명(길면 말줄임) + 스크랩 토글 아이콘(별)
  - 스크랩된 정책은 볼드 + 강조 컬러, 스크랩 안 된 정책은 기본 텍스트 (IA "캘린더 표시 규칙" 그대로)
  - 별 아이콘 클릭 시 캘린더에서 바로 스크랩 on/off 토글 가능 (정책 상세로 안 가도 됨)
  - 한 날짜에 여러 개면 칸 안에 세로로 계속 쌓임 (칸 높이는 내용에 맞춰 늘어남)
- **정책명(별 아이콘 제외한 영역) 클릭 시 정책 상세 페이지로 이동**
- 표시 대상: 로그인+프로필 있으면 매칭된 정책 전체(스크랩 여부 무관), 매칭 결과 없거나 비로그인이면 전체 정책 중 마감일 있는 것
- `deadline_type`이 "상시"인 정책은 캘린더에 표시하지 않음 (정책 정의서 규칙)
- 정책이 없는 날짜 칸은 빈 칸으로 둠 (안내 문구 없음)

**완료 조건**: 이번 달에 마감일이 있는 정책들이 해당 날짜 칸에 정확히 표시됨. 스크랩된 정책이 시각적으로 구분됨(볼드+컬러). 캘린더에서 별 아이콘 클릭 시 스크랩 상태가 즉시 반영되고 새로고침해도 유지됨. 정책명 클릭 시 해당 정책 상세로 이동함. 이전/다음 달 이동이 정상 동작함. 상시형 정책은 어떤 날짜에도 표시되지 않음.

> 추가 구현: 좌측 달력을 축소하고 **우측에 마감일 순 월별 정책 리스트**를 배치 — 기준월(8월)이 비어도 전체 마감 타임라인이 보이고(지난 마감은 흐리게+"마감", 남은 건 D-day), 리스트의 월 헤더 클릭 시 좌측 달력이 해당 월로 이동.

### 2-7. 마이페이지 - 스크랩 목록 (P1) — ✅ 구현 완료

- 로그인 필수. 스크랩한 정책만 카드 리스트로 표시 (매칭 전체가 아님)
- 카드 구성: 썸네일 또는 카테고리 배지, 정책명, 마감일, 개별 알림 아이콘(on/off 토글)
- 마감된 스크랩 정책: 카드 반투명 처리, 목록에서 자동 삭제하지 않음
- 카드 클릭(썸네일/제목 영역) → 정책 상세로 이동
- 카드 액션: "신청완료 표시"(배지 부착) / "삭제"(스크랩 목록에서 제거) — 둘 다 제공, 사용자가 선택

**완료 조건**: 스크랩한 정책만 표시됨(매칭 전체 아님). 신청완료 배지 표시와 삭제가 각각 정상 동작. 마감된 스크랩은 반투명 처리된 채 목록에 남고 자동 삭제되지 않음. 카드 클릭 시 정책 상세로 이동함.

### 2-8. 마이페이지 - 알림 설정 (P1) — ✅ 구현 완료 (실제 이메일 발송은 제외)

- 화면엔 "전체 알림 on/off" 토글 하나만 존재 (스크랩별 개별 설정은 2-7의 스크랩 목록에서 각 카드의 알림 아이콘으로 관리)
- **데이터 구조상 별도 컬럼 없음** — "전체 on/off" 클릭 시 현재 사용자의 모든 `scraps` 행의 `notification_on`을 일괄 true/false로 업데이트하는 벌크 액션으로 처리 (프로필 테이블에 새 컬럼 추가하지 않음)
- 발송 시점(마감 7일전·1일전)은 고정값, 사용자가 주기를 임의로 바꿀 수 없음 (MVP 범위)

**완료 조건**: 토글 클릭 시 그 시점에 스크랩되어 있는 모든 정책의 `notification_on`이 일괄 변경되고 DB에 반영됨. 스크랩 목록 화면에서도 개별 알림 아이콘 상태가 같이 바뀐 걸로 보임.

### 2-9. 이메일 알림 발송 (P1, 배치) — ⏸ MVP/포폴 범위 제외 (설정 UI만 제공)

- Vercel Cron으로 1일 1회 실행. 고정 기준일(`REFERENCE_DATE`) 기준으로 마감 7일전 또는 1일전인 정책을 스크랩한 사용자에게 Resend로 메일 발송
- `notification_on=false`인 스크랩은 발송 대상에서 제외
- `deadline_type`이 "상시"인 정책은 발송 대상에서 제외 (마감일 자체가 없음)
- 발송 실패 시 재시도 로직은 MVP 범위 밖 — 실패 로그만 남기고 다음날 배치에서 계속 진행

**완료 조건**: 기준일 기준으로 마감 7일전·1일전에 해당하는 스크랩 보유 사용자에게 메일이 실제로 발송됨. 알림을 꺼둔 사용자에게는 발송되지 않음. 상시형 정책은 발송 트리거에서 제외됨.

---

## 3. 비즈니스 로직

상세 규칙(필드별 매칭 규칙, AND 결합, 소득 근사 규칙, 예외 처리, 정렬 규칙)은 `docs/프로젝트_로드맵.md`의 "정책 정의서 (필터링/매칭 로직)" 섹션이 SSOT(Single Source of Truth). 코드 구현 시 반드시 그 섹션 기준으로 판단.

---

## 4. 데이터 모델

`profiles` / `policies` / `scraps` 3개 테이블. 필드 정의는 `docs/프로젝트_로드맵.md`의 "데이터 구조 설계" 섹션 참고. 실제 스키마는 `supabase/migrations/`에 SQL로 반영되어 있음.

---

## 5. 기술 스택 · 실행 규칙

스택: Next.js + TypeScript + Tailwind + Supabase (자세한 건 CLAUDE.md)

실제 구현 메모: 캘린더는 `react-day-picker` 대신 **자체 구현**. 이메일(Resend)·Vercel Cron은 2-9(발송) 제외로 **현재 미사용**(추후 확장 시 도입).

**배포**: GitHub `jungbin9999/checkngo`(public) → Vercel 자동 배포. 라이브 **https://checkngo.vercel.app**. `main`에 push하면 자동 재배포됨. 환경변수(`NEXT_PUBLIC_SUPABASE_URL`·`NEXT_PUBLIC_SUPABASE_ANON_KEY`)는 Vercel 프로젝트 설정에 등록(코드/깃에는 없음). Supabase **Confirm email OFF**(가입 즉시 로그인).

커밋 전 확인: `npx tsc --noEmit` / `npm run lint` / `npm run build` 모두 통과해야 함

---

## 6. 경계 설정

**항상 해도 됨**
- 파일 읽기, 코드 조회, `npm run dev`로 로컬 서버 실행
- 단순 조회성 명령(git status, ls 등)

**물어보고 진행**
- 3개 이상 파일에 걸친 스키마/구조 변경 (Plan Mode 사용 권장)
- DB에 실제 데이터 넣거나 바꾸는 작업 (SQL 파일 먼저 만들고 검토 후 실행)
- 새 패키지 설치

**절대 하면 안 됨**
- Supabase 서비스 키(service_role/secret)를 프론트엔드 코드나 `NEXT_PUBLIC_` 환경변수에 넣기
- `new Date()`로 마감/기준일 판단 (반드시 고정된 `REFERENCE_DATE` 사용 — CLAUDE.md 참고)
- 실제 DB에 대한 파괴적 명령(테이블 삭제 등)을 확인 없이 실행

---

## 7. 배포 후 부가 작업 (포폴 마감)

- **데모 계정**: `demo@checkngo.app` / `demo1234` (공개용). 프로필(25~29·미취업·100~200·월세·서울 → 매칭 9건) + 스크랩 3건(진행중 2·신청완료 1)이 시드돼 있음. 로그인 화면 "데모로 둘러보기" → 자동 로그인. 데모 로그인 시 헤더에 안내 배너. (지저분해지면 시드 스크립트로 리셋 가능)
- **파비콘**: 브랜드 아이콘 `src/app/icon.svg` (Next 기본 favicon 대체).
- **링크 미리보기(OG) 카드**: `src/app/opengraph-image.tsx` — `next/og`로 1200×630 동적 생성(로고 이미지 + 후킹멘트 "내 조건에 맞는 청년 정책, 한눈에 확인하기" + 앱 정책 카드). **한글은 서브셋 폰트 `assets/og-font.ttf` 번들**(카드 문구를 바꾸면 서브셋 폰트도 갱신해야 함 — 안 그러면 새 글자가 □로 깨짐). `layout` 메타데이터에 `openGraph`/`twitter`/`metadataBase`.
- **커스텀 404**: `src/app/not-found.tsx` (브랜드 + "홈으로").
- **README**: 공개 저장소용 소개 `README.md`.
- **케이스 스터디**: `docs/케이스스터디.md` (문제정의 → 기능 → 개발 과정 → 의사결정 → 트러블슈팅 → 회고).
- **배포 후 UX 폴리싱**: 로그인 가치제안 문구·데모 버튼 강조, 홈 상단 "데이터 기준일(2026.08.15)" 안내 배너, 모바일 캘린더는 셀에 점 표시(상세는 우측 월별 리스트), 정책 상세의 소득 기준(원문) 별도 박스 분리, 헤더 360px 오버플로우 수정.

---

## 진행 상황 체크

- [x] 2-0. 공통 헤더/네비게이션 — 공통 `Header` 컴포넌트 추출, 로고→홈·캘린더·마이페이지 진입점 완료
- [x] 2-1. 로그인/회원가입 — 이메일 형식 검증 포함
- [x] 2-2. 프로필 입력
- [x] 2-3. 홈 - 조건 매칭 필터링
- [x] 2-4. 정책 상세
- [x] 2-5. 공통 UI 상태 규칙 — 로딩/에러/반응형 점검·수정 완료(헤더 360px 오버플로우 수정, raw 에러 문구 친화화)
- [x] 2-6. 캘린더
- [x] 2-7. 마이페이지 - 스크랩 목록 — 카드별 알림 토글 포함 완료
- [x] 2-8. 마이페이지 - 알림 설정 — 전체 on/off(`notification_on` 벌크) 완료. 실제 이메일 발송은 제외
- [ ] 2-9. 이메일 알림 발송 (배치) — MVP/포폴 범위에서 제외 (설정 UI만 제공, 발송 인프라 생략)

> ※ 로그인 필요 DB 반영(프로필 저장·스크랩·체크리스트·캘린더 별 토글·알림 on/off)은 브라우저 E2E로 **검증 완료** (2-2·2-4·2-6·2-7·2-8).